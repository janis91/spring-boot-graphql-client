query SearchForRepositories($query: String!, $first: Int) {
  search(query: $query, type: REPOSITORY, first: $first) {
    nodes {
      ... on Repository {
        name
        url
        description
      }
    }
  }
}
